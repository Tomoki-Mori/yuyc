<%= form_for @result do |r| %>
    <%= r.label :competition %>
    <%= r.text_field :competition %>

    <%= r.label :picture1 %>
    <%= r.file_field :picture1, :size => 140 %>

    <%= r.label :body %>
    <%= r.text_field :body, :size => 140 %>

    <%= r.submit "編集する" %>
<% end %>